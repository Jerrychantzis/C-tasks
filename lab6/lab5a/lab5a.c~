#include <stdio.h>
#include <stdlib.h>


int main(){

	FILE *dt; *pos; *neg; 	
	int *pinakas; 			/*o pinakas pou tha balw to arxeio*/
	int i; 
	int length;

	if( (dt = fopen("data.txt", "r") ) == NULL){
		return 1;
	}
	
	if( (pos = fopen("positive.txt", "w") ) == NULL)
		return 1;
	}

	if( (neg = fopen("negative.txt", "w") ) == NULL)
		return 1;
	}

	fscanf(dt, "%d", &length); /*pairnw to prwto stoixeio gia length*/

	pinakas =(int *)malloc(length * sizeof(int));

	for(i=0; i<length; i++){
		fscanf(dt, "%d", &pinakas



	return 0;

}
