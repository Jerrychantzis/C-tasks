#include <stdio.h>
#include <unistd.h>
#include <stdlib.h>
#include <sys/wait.h>

int main() {

	FILE *positive;
	int num;

	if(pipe(pfd) < 0){
		return (1);
	}
	
	if(fork() == 0){
		close (pfd[0]);
		write (pfd[1], 

	else{
		close (pfd[1]);
		


	
